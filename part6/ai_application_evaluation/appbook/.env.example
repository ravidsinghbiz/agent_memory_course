# The deterministic regression appbook needs no model or database credentials.
# Override these only when changing the local evidence store or presentation pace.
AIEVAL_DATABASE_PATH=.data/evaluation_runs.sqlite3
AIEVAL_SUITE_DELAY_MS=70
PORT=8006
